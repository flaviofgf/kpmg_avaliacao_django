services:
  django:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - '8000:8000'
  #    depends_on:
  #      - db
  spark:
    ports:
      - '8080:8080'
  spark-worker:
    ports:
      - '8081:8081'
